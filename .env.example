# Mapbox (map + optional forward geocoding fallback)
NEXT_PUBLIC_MAPBOX_TOKEN=

# OpenAI — server only
OPENAI_API_KEY=

# Optional: Supabase for saved studies (future)
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
